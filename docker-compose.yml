version: '3.8'

services:
  mock-server:
    image: mock-server:latest
    container_name: mock-server
    ports:
      - "8080:8080"
    environment:
      - GIN_MODE=release
    restart: always
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
